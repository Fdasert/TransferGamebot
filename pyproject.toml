[project]
name = "transfer-gamebot"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "python-telegram-bot==21.6",
    "supabase==2.9.1",
    "httpx[http2]==0.27.2",
    "requests==2.32.3",
    "fastapi==0.115.0",
]

[tool.vercel]
entrypoint = "main:app"
